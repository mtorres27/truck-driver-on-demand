= render partial: "/company/shared/profile_sub_nav"

- content_for :title, "AVJunction - Profile"

- content_for :secondary do
  .action-bar
    h2.heading-h2.heading-h2--action-bar Profile Info
    .action-bar--actions
      a.btn.btn--primary href=edit_company_profile_path
        | Edit


  - if @company.profile_header_url.include?("missing") or @company.header_source == "color"
    .profile__wallpaper style="background-color: #{ @company.header_color.include?("#") ? "" : "#"}#{@company.header_color}"
  - else
    .profile__wallpaper style="background-image: url(#{@company.profile_header_url.include?("missing") ? "" : @company.profile_header_url});"


  = render "shared/profile-company"

