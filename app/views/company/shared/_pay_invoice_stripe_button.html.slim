= form_tag company_job_contract_pay_path(job), method: 'post' do
  input type="hidden" name="quote_id" value=payment.id
  script.stripe-button data-label="Pay Invoice" data-amount="#{ (payment.amount.floor + (payment.amount * job.applicable_sales_tax / 100)) * 100 }" data-image="/assets/stripe-logo.png" data-email=("#{current_user.email}") data-description="Pay Invoice" data-key=("#{Rails.configuration.stripe[:publishable_key]}") data-locale="auto" src="https://checkout.stripe.com/checkout.js" data-currency="#{job.currency}" data-panel-label="Pay Invoice"