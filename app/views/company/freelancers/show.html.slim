.container.container--header
  - if @freelancer.freelancer_profile.profile_header_url.include?("missing") or @freelancer.freelancer_profile.header_source == "color"
    .profile__wallpaper.profile__wallpaper--wide style="background-color: #{ @freelancer.freelancer_profile.header_color.include?("#") ? "" : "#"}#{@freelancer.freelancer_profile.header_color}"
  - else
    .profile__wallpaper.profile__wallpaper--wide style="background-image: url(#{@freelancer.freelancer_profile.profile_header_url.include?("missing") ? "" : @freelancer.freelancer_profile.profile_header_url});"


.container.container--profile
  .frame
    .action-bar
      h2.heading-h2.heading-h2--action-bar
        = @freelancer.first_name_and_initial
      .action-bar__actions
        - if @freelancer.freelancer_profile.available
          .tag.tag--success.gutter-right
            | Available
          .btn.btn--primary onclick="togglePopOver()" Invite to quote
          .popover--blanket onclick="hidePopOver()"
          .popover

            label.form__field-label for="field"  Invite to quote
            select.form__select-field.w-select name="job_to_invite" id="job_to_invite"
              option value=""
                | Select Job
              - @jobs.each do |job|
                option value="#{job.id}"
                  | #{job.title}
            .btn.btn--primary id="job_invite_button" style="cursor: pointer" onclick="submitInvitation(#{@freelancer.id})" class="btn btn--primary btn--primary--disabled"
              div invite

            span.popover-response--error
            span.popover-response--success
        - else
          .tag.tag--warning
            | Unavailable
        - if @favourite
          a.btn-icon-link href="#{params[:id]}?toggle_favourite=true"
            div class="zmdi zmdi-favorite"
        - else
          a.btn-icon-link href="#{params[:id]}?toggle_favourite=true"
            div class="zmdi zmdi-favorite-outline"

    = render "shared/profile-freelancer"
