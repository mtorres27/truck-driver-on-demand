/ - if request.env['PATH_INFO'] != "/confirm_email"
/   - flash.each do |type, message|
/     div class="alert #{alert_class_for(type)} alert-dismissible mb-0"
/       button type="button" class="close" data-dismiss="alert"
/         span aria-hidden="true" &times;
/         span.sr-only Close
/       = message
