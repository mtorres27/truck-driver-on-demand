.row
  .col-md-9.col-12
    = f.input :name, as: :string, placeholder: "Project Name", label: "Project Name", label_html: { class: "truckker-caption1" }
  .col-md-3.col-8
    .form-group
      label.truckker-caption1
        | Project Image
      - if !f.object.image_url&.include?('missing.png') && f.index != 'new_freelancer_portfolios'
        img.mb-1.authorable_attachment--full src=f.object.image_url
      - else
        img.mb-1.authorable_attachment--full src=""
      .d-flex.justify-content-between
        - if !f.object.image_url.include?('missing.png') && f.index != 'new_freelancer_portfolios'
          a.zmdi.zmdi-camera onclick="uploadPortfolioImage(#{f.index})"
        - else
          a.zmdi.zmdi-camera onclick="uploadPortfolioImage()"
        = link_to_remove_association content_tag(:span, "", class: "zmdi zmdi-delete"), f, { onClick: "hide_remove_association(this)"}
  = f.file_field :image, label: false, :accept => 'image/png,image/gif,image/jpeg', class: "truckker-hidden-important file-input--hidden"
